
// Demo 2) Getting HTML/A View as a result and just throwing it in to the response div

/*
$('#cnn').click(function () {
    $.ajax({
        type: 'POST',
        url: '/test',
        success: function (response) {
            $('#results').html(response);
        },
        data: {
            format: 'html',
            query: $('input[name=add-CNN]').val(),
            _token: $('input[name=_token]').val(),
        },
    });
});
*/
            